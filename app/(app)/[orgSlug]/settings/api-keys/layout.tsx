import { Metadata } from "next";

export const metadata: Metadata = {
  title: "API Keys | OpenGrove",
  description: "Manage API keys for external agents and integrations.",
};

export { default } from "./page";
