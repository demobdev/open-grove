Yes — the homepage needs to evolve if semantic GitHub linking, repo intelligence, and agentic workflows are becoming part of the product story.

Right now the homepage is already strong. It says:

fast issue tracker + boards + cycles + AI agent + keyboard-first UX

That is clean. But now the bigger story is becoming:

OpenGrove is the issue tracker that stays synced with the codebase.

That is a sharper wedge. Linear/Jira already have issue tracking. Your differentiator is the board reflects reality without babysitting it.

The big positioning upgrade

Current hero:

The issue tracker built for speed

Still good. Keep it.

But the supporting copy should evolve from “fast product teams plan, track, and ship” to something more self-maintaining / code-aware.

Better hero subhead options

Option 1 — strongest product angle

OpenGrove is the keyboard-first issue tracker that stays synced with your codebase — linking PRs, commits, cycles, and agent activity without the busywork.

Option 2 — cleaner and punchier

Plan the work. Ship the code. OpenGrove keeps the board in sync.

Option 3 — more AI-native

A fast issue tracker for human and AI engineering teams — with code-aware automation that links work, updates status, and keeps momentum visible.

Option 4 — investor/product-market-fit style

OpenGrove turns issues, pull requests, commits, and cycles into one live system of record for modern software teams.

My favorite for the top:

The issue tracker built for speed
Plan work, ship code, and let OpenGrove keep the board in sync — with semantic GitHub linking, live cycles, and an AI agent that handles the busywork.

That keeps your existing brand but adds the new power.

⸻

New homepage sections I would add

Your current page has:

01 · Issues
02 · Board & Cycles
03 · AI Agent
04 · Keyboard-first
05 · Everything else
Testimonials
CTA

I would modify it to:

01 · Issues
02 · Board & Cycles
03 · GitHub Sync
04 · AI Agent
05 · Command Center
06 · Everything else
Testimonials
CTA

The missing section is GitHub Sync / Code-aware tracking. That is now core.

New section: GitHub Sync

Section label

03 · GitHub Sync

Heading options

Option 1

Your board knows what shipped

Option 2

The tracker that reads the codebase

Option 3

PRs, commits, and issues — finally in sync

Option 4

No ticket key? No problem.

Option 5

Work links itself

My pick:

Work links itself

Then subcopy:

OpenGrove connects issues to pull requests and commits automatically — even when a developer forgets the issue key. Explicit references link instantly. Semantic matching catches the rest.

Feature cards for this section

Semantic PR linking

Open a pull request without ENG-142 in the title. OpenGrove compares the PR, description, commits, and files changed against active issues and links the right work.

Commit-aware activity

Commits roll into the issue timeline, so every status update has evidence behind it.

Smart status movement

PR opened? Move to In Progress. Ready for review? Move to In Review. Merged? Follow your team’s workflow — not a hardcoded guess.

Review before automation

High-confidence matches link automatically. Unclear matches land in Inbox as suggestions your team can approve.

That last one is important. It makes the AI feel safe.

⸻

UI mock copy for GitHub section

You could show a card like this on the homepage:

GitHub activity detected
PR #218
feat: add fuzzy repo selector to onboarding
Likely match
ENG-147 · Add searchable repository picker
94% confidence
Actions
✓ Linked pull request
✓ Moved issue to In Progress
✓ Added activity comment

Then another smaller card:

Needs review
Commit a81f3c
"fix broken state"
Possible match
ENG-139 · Fix websocket reconnect after laptop resume
82% confidence
Approve link
Ignore

This makes the value obvious in five seconds.

Better copy for AI Agent section

Your current AI section is good, but now the agent should not only be “inside your workspace.” It should understand GitHub activity too.

Current:

OpenGrove’s agent works inside your workspace with org-scoped tools — it files issues, summarizes cycles, drafts standups, and catches duplicates before they ship.

Updated:

OpenGrove’s agent works across your issues, cycles, projects, and GitHub activity — filing issues, summarizing progress, catching duplicates, and surfacing work that moved without a ticket.

Feature cards:

Repo-aware answers

Ask what changed in a repo, what shipped this week, or which PRs are blocking the cycle.

Standups from real work

Generate updates from issue movement, PRs, commits, and comments — not memory and vibes.

Unlinked work detector

Find commits and PRs that slipped past the board before they become mystery meat in the release notes.

Duplicate radar

Semantic search catches issues that describe the same work before the backlog turns into a junk drawer with buttons.

⸻

Better copy for Keyboard-first section

Right now “Your hands never leave the keyboard” is great. I’d evolve it into Command Center, because Command-K is one of your strongest UI advantages.

Section label

05 · Command Center

Heading

One command bar for the whole workspace

Subcopy

Create issues, switch repos, search projects, ask the agent, approve AI suggestions, and jump across teams without touching the sidebar.

Command examples

connect github repo
show unlinked PRs
summarize current cycle
create issue from PR #218
switch to open-grove-web
ask agent what shipped yesterday
move merged PR issues to review

That makes Command-K feel less like a shortcut menu and more like the operating system of the app.

⸻

Homepage section order I recommend

Hero

Main message: speed + code-aware sync.

Product mockup

Keep the big dashboard screenshot. But update the fake issues to include GitHub/AI examples:

ENG-147
Migrate issue search to the vector index
PR #218 linked · In review
ENG-146
Cycle burndown drifts across weekend boundaries
Blocked · Needs data backfill
ENG-135
Stream agent tool calls into activity feed
Merged · Done

01 · Issues

Keep mostly the same.

02 · Board & Cycles

Keep mostly the same.

03 · GitHub Sync

New section. This is the big add.

04 · AI Agent

Update to be repo-aware.

05 · Command Center

Make Command-K more ambitious.

06 · Everything else

Keep your table stakes section.

⸻

New landing page copy draft

Here’s a clean version of the new flow.

Hero

New
Semantic GitHub linking is here
The issue tracker built for speed
Plan work, ship code, and let OpenGrove keep the board in sync — with keyboard-first issues, live cycles, semantic PR linking, and an AI agent that handles the busywork.
Free for teams of 3 · No credit card · Press ⌘K anywhere

Alternative badge:

New
Your board now understands your repos

That badge is stronger than “AI agent joins your team on Pro,” honestly.

⸻

GitHub Sync section copy

03 · GitHub Sync
Work links itself
OpenGrove connects issues to pull requests and commits automatically. Use issue keys when you want. When someone forgets, semantic matching catches the work and keeps the tracker honest.

Feature cards:

Explicit when you have it
Branches, PRs, and commits with ENG-142 link instantly.
Semantic when you forget it
No issue key? OpenGrove compares the PR title, description, commits, and files changed against active issues.
Status without babysitting
PR opened, ready for review, or merged — OpenGrove moves the issue according to your team’s workflow.
Suggestions when unsure
Low-confidence matches go to Inbox for approval instead of making a mess.

Mock UI card:

PR #218 opened
feat: add fuzzy repo selector to onboarding
Matched to
ENG-147 · Add searchable repository picker
Confidence
94%
Done
Linked PR
Moved issue to In Progress
Posted activity comment

⸻

AI Agent section updated copy

04 · AI Agent
An agent that understands the work
OpenGrove’s agent reads your issues, cycles, projects, and GitHub activity with org-scoped permissions — so it can summarize what shipped, spot risk, create follow-up issues, and keep the team moving.

Feature cards:

Ask about the repo
“What changed in open-grove this week?” gets answered from PRs, commits, and linked issues.
Cycle reports on demand
Generate standups and cycle summaries from real activity, not scattered Slack memory.
Find work that slipped
The agent surfaces PRs and commits that were never connected to an issue.
Create next actions
Turn blockers, TODOs, and review notes into issues without leaving the flow.

⸻

Command Center section updated copy

05 · Command Center
One shortcut for everything
OpenGrove’s command bar is more than navigation. Create issues, switch repos, approve AI suggestions, ask the agent, and move work across the system from one place.

Command menu examples:

Create issue
Connect GitHub repo
Show unlinked PRs
Switch repository
Ask agent about this cycle
Create issue from PR
Approve suggested link
Summarize what shipped

⸻

“Everything else” section additions

Your current section says:

Full-text search
Saved views
Attachments
Mentions & threads
Live presence
Multi-team workspaces

I’d add or swap in:

Repo mapping
Connect multiple repositories to a team or project and keep work scoped to the right codebase.
AI Suggestions Inbox
Review uncertain matches, duplicate warnings, and follow-up tasks before they touch the board.
Activity timeline
Every issue shows status changes, comments, PRs, commits, and agent actions in one history.
Org-scoped permissions
Agents and automations only act inside the teams, repos, and workspaces they’re allowed to access.

Maybe replace “Attachments” with “Repo mapping” if the grid stays six cards. Attachments are table stakes; repo mapping is strategy.

⸻

Better testimonial ideas

Your testimonials are fake/product placeholders anyway, so make them sell the new differentiator.

“OpenGrove caught PRs our team forgot to link and moved the right issues before standup. It’s the first tracker that feels like it’s paying attention.”
Mara Lindqvist
VP Engineering · Tidal
“We stopped asking ‘what’s the status?’ because the board finally matched the repo.”
Theo Park
Engineering Lead · Aperture
“Our juniors don’t have to memorize every workflow rule, and our seniors don’t have to clean up after them. That alone sold it.”
Ada Okafor
CTO · Hexa Labs

⸻

Strong CTA options

Current:

Stop managing your tracker. Start shipping.

That is already excellent.

A slightly more code-aware version:

Stop syncing your tracker by hand. Start shipping.

Subcopy:

Connect your repos, create your first team, and let OpenGrove keep issues, PRs, commits, cycles, and agent activity in one fast workspace.

Button:

Get started free

Secondary:

Connect GitHub

⸻

Everything you are adding to the product story

Here’s the clean list:

Semantic GitHub linking
Automatic PR-to-issue matching
Automatic commit-to-issue matching
Explicit issue key support
Confidence scoring
AI Suggestions Inbox
Repo-to-team mapping
Repo-to-project mapping
Multi-repo organizations
GitHub activity timeline on issues
Smart status movement from PR events
Org-level automation settings
Agent summaries from GitHub + issue activity
Unlinked PR/commit detection
Duplicate issue detection
Command-K repo switching
Command-K AI actions
Command-K GitHub actions
Cycle health from real GitHub activity
Release/recent-shipped summaries

That is a real roadmap. Not fluff.

The one-liner you should keep repeating

OpenGrove keeps your board synced with the codebase.

That is the spine.

Everything else supports that:

Issues = planned work
GitHub = actual work
AI agent = connective tissue
Command-K = control layer
Cycles = momentum

That is the product narrative.

My recommended homepage change

Do not overhaul the whole page. It already looks expensive, which is half the battle. Just insert one new section and tune the copy.

The biggest wins:

1. Change hero subhead to mention codebase sync.
2. Add a new GitHub Sync section after Board & Cycles.
3. Update AI Agent copy to include repo/GitHub activity.
4. Rename Keyboard-first to Command Center or expand its scope.
5. Add “AI Suggestions Inbox” and “Repo mapping” to table stakes.
6. Update CTA to “Stop syncing your tracker by hand.”

That is enough. Don’t accidentally turn the homepage into a feature yard sale. The product should still feel calm, fast, and inevitable.
